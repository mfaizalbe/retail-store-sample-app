grafana:
  enabled: true
  service:
    type: LoadBalancer
  adminPassword: ${grafana_admin_password}
  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      labelValue: "1"
      searchNamespace: ALL
